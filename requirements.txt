Django>=4.2,<5.0
psycopg2-binary>=2.9.9     # PostgreSQL support (optional, remove if not needed)
mysqlclient>=2.2.4         # MySQL support (optional, remove if not needed)
djangorestframework>=3.15  # Optional: for building APIs
django-crispy-forms>=2.2   # For better form rendering
django-filter>=24.2        # Filtering in lists
gunicorn>=21.2.0           # WSGI server for production
whitenoise>=6.7.0          # Serve static files in production
python-dotenv>=1.0.1       # Load environment variables
